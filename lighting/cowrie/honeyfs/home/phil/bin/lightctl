#!/bin/sh

echo 'light on/off'
if [ "$1" = "on" ]; then
    echo 'turning light on'
    # Add command to turn the light on
elif [ "$1" = "off" ]; then
    echo 'turning light off'
    # Add command to turn the light off
else
    echo 'Usage: lightctrl on|off'
fi
